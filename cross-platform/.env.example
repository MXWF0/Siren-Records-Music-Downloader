# Leave empty when the frontend and /api functions are deployed together.
# For a separate static host, set this to the proxy origin, for example:
# VITE_API_BASE_URL=https://your-siren-proxy.example
VITE_API_BASE_URL=
